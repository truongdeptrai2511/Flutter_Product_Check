import 'package:first_project_g1/model/ProductsModel.dart';
import 'package:flutter/cupertino.dart';

class ProductsContent extends StatelessWidget {
  const ProductsContent({super.key, required this.data});
  final ProductsModel data;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
